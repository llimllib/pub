from pub import task
from pub.shortcuts import python, pip, rm

@task
def clean():
    rm("-rf build dist pub.egg-info docs")

@task
def build():
    python("setup.py build")

@task
def install():
    python("setup.py install")
