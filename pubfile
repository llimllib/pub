from pub import task
from pub.shortcuts import python, pip, rm

@task
def clean():
    pip("uninstall pub")
    rm("-rf build dist pub.egg-info")

@task
def build():
    python("setup.py build")

@task
def install():
    python("setup.py install")
